<% ['Targets', 'History', 'Profile', 'Payment'].each_with_index do |tab,index| %>
  <% if @active == tab.downcase%> <%=tab%> <%else%> <%=link_to tab, self.send("sales_#{tab.downcase}_path") %> <%end%>
<% concat '|' if index < 3 %>
<% end %>
